name: Makefile CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install dependencies
      run: make poetry

    - name: Run lint
      run: make lint